//pointer arithmetic using arrays
#include<stdio.h>
#include<conio.h>
void main()
{
int a[20],n,sum=0,i;
int*p;
clrscr();
printf("enter value for n");
scanf("%d",&n);
for(i=0;i<n;i++);
{
printf("enter array elements");
scanf("%d",&a[i]);
}
p=&a[0];
for(i-o,i<n;i++)
{
//sum=sum + a[i];
sum=sum = *p;
p++;
}
printf("sum=%d\n",sum);
getch();
}
